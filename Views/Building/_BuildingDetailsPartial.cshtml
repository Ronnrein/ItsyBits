@model Building
<h1>Animals in "@Html.ActionLink(Model.Name, "Details", "Building", new { id = Model.Id })" building</h1>
<table class="table">
    @foreach (Animal animal in Model.Animals) {
        @Html.Partial("/Views/Animal/_animalDetailsRow.cshtml", animal)
    }
</table>