@model Building
@{ ViewData["Title"] = "Building details"; }
<div class="ui stackable two column grid">
    <div class="column info-column">
        <div class="ui segments">
            <div class="ui raised segment secondary">
                <h4>@Model.Name</h4>
            </div>
            <div class="ui raised segment">
                <p class="ui orange ribbon label">Statistics</p>
                <div class="ui four small statistics">
                    <div class="statistic">
                        <div class="value">
                            @Model.Animals.Count
                        </div>
                        <div class="label">
                            animals
                        </div>
                    </div>
                    <div class="statistic">
                        <div class="value">
                            @Model.Capacity
                        </div>
                        <div class="label">
                            capacity
                        </div>
                    </div>
                    <div class="statistic">
                        <div class="value">
                            @Model.Reward
                        </div>
                        <div class="label">
                            value
                        </div>
                    </div>
                    <div class="statistic">
                        <div class="value">
                            @Model.BuildingUpgrades.Count
                        </div>
                        <div class="label">
                            upgrades
                        </div>
                    </div>
                </div>
            </div>
            <div class="ui raised segment">
                <p class="ui orange ribbon label">Upgrades</p>
                <div class="upgrades clearfix" >
                    <div class="ui inline simple dropdown">
                        <div class="text">
                            <img class="ui avatar image" src="~/images/upgrades/upgrade.png" />
                        </div>
                        <div class="menu vertical basic buttons">
                            @foreach (Upgrade upgrade in (IEnumerable<Upgrade>)ViewData["Upgrades"]) {
                                <form asp-controller="Store" asp-action="BuildingUpgrade" asp-route-id="@upgrade.Id">
                                    <input type="hidden" name="BuildingId" value="@Model.Id" />
                                    <button type="submit" class="ui item basic button tooltip" data-content="@upgrade.Description" data-position="right center" name="UpgradeId" value="@upgrade.Id">
                                        <img class="ui avatar image" src="~/images/upgrades/@upgrade.SpritePath" />
                                        @upgrade.Name
                                    </button>
                                </form>
                            }
                        </div>
                    </div>
                    @foreach (Upgrade upgrade in Model.Upgrades) {
                        <div>
                            <img class="ui avatar image image tooltip" data-content="@upgrade.Description" src="~/images/upgrades/@upgrade.SpritePath" />
                        </div>
                    }
                </div>
            </div>
            <div class="ui raised segment">
                <p class="ui orange ribbon label">Description</p>
                <p>@Model.Type.Description</p>
            </div>
            <div class="ui raised segment">
                <p class="ui orange ribbon label">Status</p>
                <p>@Model.GetStatusText()</p>
            </div>
            <div class="ui raised segment">
                <div class="ui small indicating progress progressbar tooltip" data-content="@Model.HappinessPercentage%" data-value="@Model.HappinessPercentage" data-total="100">
                    <div class="bar"></div>
                    <div class="label">Happiness</div>
                </div>
            </div>
        </div>
    </div>
    <div class="column">
        <div class="ui raised segment ray-image-container">
            <img class="ui centered big image" src="~/images/buildings/@Model.Type.SpritePath/full.png"/>
        </div>
    </div>
</div>
<div class="ui segment secondary mid-segment-header">
    <h4>Animals in building</h4>
</div>
<div class="ui cards">
    @foreach(Animal animal in Model.Animals) {
        @Html.Partial("~/Views/Animal/_AnimalCardPartial.cshtml", animal)
    }
</div>