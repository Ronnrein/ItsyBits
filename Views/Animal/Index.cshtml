@model IEnumerable<Animal>
@{ ViewBag.Title = "My animals"; }

<h1>My animals</h1>
<table class="table">
    @foreach (Animal animal in Model) {
        @Html.Partial("_AnimalDetailsRow", animal)
    }
</table>